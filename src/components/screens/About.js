import React from "react";
import { Text, View } from "react-native";

const About = ({ navigation }) => {
  return (
    <View>
      <Text>About</Text>
    </View>
  );
};

export default About;
